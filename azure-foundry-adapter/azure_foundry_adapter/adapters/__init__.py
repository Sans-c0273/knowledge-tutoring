"""One adapter module per sibling project, each matching that project's own
provider interface exactly so it can be dropped straight into that project's
package tree. See the top-level README for which file goes where."""
